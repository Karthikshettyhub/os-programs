Creating the Employee table
CREATE TABLE Employee (
EMPNO INT , ENAME VARCHAR(50), JOB VARCHAR(50),
MANAGER_NO INT, SAL DECIMAL(10,2),
COMMISSION INT );

SQL> DESC Employee;
2. Inserting three records into the Employee table and using
rollback:
-- Inserting records

INSERT INTO Employee VALUES (2, 'Pavan', 'Developer', 1,
4000.00, 500.00);

INSERT INTO Employee
VALUES (3, 'Alice', 'Analyst', 1, 3500.00, NULL);
SQL> SELECT * FROM EMPLOYEE;
3. Add primary key constraint and not null constraint to the
employee table.

ALTER TABLE employee
ADD PRIMARY KEY (EMPNO);

SQL> desc employee;
4. Insert null values to the employee table and verify the
result.
-- Inserting a record with null values
INSERT INTO Employee
VALUES (4, NULL, 'Intern', NULL, NULL, NULL);

-- Verifying the result
SELECT * FROM Employee;
